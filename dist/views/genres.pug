extends base.pug

block body
    .container(class="mt-2")
        .row 
            .col-12
                h3 Genre list
            each genre in genres
                .col-2(class="mb-3")
                     a(href="/genres/"+genre.id class="text-decoration-none w-100 text-success") #{genre.name}           